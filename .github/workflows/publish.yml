name: Publish to Maven Central

on:
  push:
    tags:
      - 'v*'  # Triggers on tags like v1.0.0, v0.3.0, etc.

jobs:
  publish:
    runs-on: macos-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          distribution: temurin
          java-version: '17'

      - name: Grant execute permission to gradlew
        run: chmod +x gradlew

      - name: Import GPG Key
        run: |
          # Import the passphrase-free GPG private key into the keyring.
          echo "$GPG_PRIVATE_KEY" | gpg --batch --import
          # (Optional) List imported keys for debugging.
          gpg --list-secret-keys --keyid-format LONG
        env:
          GPG_PRIVATE_KEY: ${{ secrets.GPG_PRIVATE_KEY }}

      - name: Build and Publish to Maven Central
        env:
          RELEASE_SIGNING_ENABLED: true
          MAVEN_CENTRAL_USERNAME: ${{ secrets.MAVEN_CENTRAL_USERNAME }}
          MAVEN_CENTRAL_PASSWORD: ${{ secrets.MAVEN_CENTRAL_PASSWORD }}
          # Optionally, provide the key ID if needed. With useGpgCmd(), GPG will use your default key if only one is available.
          signing.keyId: ${{ secrets.SIGNING_KEY_ID }}
        run: |
          ./gradlew publish
