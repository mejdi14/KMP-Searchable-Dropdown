package io.github.mejdi14.searchabledropdown.data.search

/** Where the search input is shown. */
enum class SearchLocation {
    /** At the top of the popup content (default). */
    POPUP,

    /** In the header itself: the header turns into the search field while the popup is open. */
    HEADER
}
